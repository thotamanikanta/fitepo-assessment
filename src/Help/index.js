import { Card, CardContent, Typography } from "@mui/material";

const Help = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h2">Help</Typography>
      </CardContent>
    </Card>
  );
};

export default Help;

import { Card, CardContent, Typography } from "@mui/material";

const Statictics = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h2">Statictics</Typography>
      </CardContent>
    </Card>
  );
};

export default Statictics;
